# HDS Belgium – Modern Website (Next.js)

## Run locally
1) Install Node.js (LTS)
2) In this folder:
```bash
npm install
npm run dev
```
Open http://localhost:3000

## Deploy
- Vercel: import and deploy
- Any Node hosting: `npm run build` then `npm run start`

## Edit content
- Products: `data/products.ts`
- Translations: `messages/en.json`, `messages/fr.json`, `messages/de.json`
- PDFs: `public/pdfs/`
- Images: `public/images/`
